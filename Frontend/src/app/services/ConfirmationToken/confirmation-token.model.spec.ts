import { ConfirmationToken } from './confirmation-token.model';

describe('ConfirmationToken', () => {
  it('should create an instance', () => {
    expect(new ConfirmationToken()).toBeTruthy();
  });
});
